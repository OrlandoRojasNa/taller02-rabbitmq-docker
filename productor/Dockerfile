# Etapa 1: compilar con Maven + JDK dentro del contenedor (nada se instala en el equipo)
FROM maven:3.9-eclipse-temurin-21 AS build
WORKDIR /app
COPY pom.xml .
RUN mvn -B -q dependency:go-offline
COPY src ./src
RUN mvn -B -q package

# Etapa 2: imagen liviana solo con el JRE y el .jar
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY --from=build /app/target/productor.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]
