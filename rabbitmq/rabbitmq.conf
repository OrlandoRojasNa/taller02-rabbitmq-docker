# Carga usuario, permisos y la cola durable al arrancar el nodo.
# Asi la cola existe desde el primer arranque, sin crearla a mano en la consola.
load_definitions = /etc/rabbitmq/definitions.json

default_user = lab
default_pass = lab123
